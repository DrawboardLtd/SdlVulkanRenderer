<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <!-- Allow floating versions (e.g. "4.4.*") so the DIR.Lib pin auto-picks
         up new CI publishes without manual bumps here. -->
    <CentralPackageFloatingVersionsEnabled>true</CentralPackageFloatingVersionsEnabled>
  </PropertyGroup>

  <ItemGroup>
    <PackageVersion Include="DIR.Lib" Version="6.14.*" />
    <PackageVersion Include="Microsoft.SourceLink.GitHub" Version="10.0.300" />
    <PackageVersion Include="SDL3-CS" Version="3.5.0-preview.20260213-150035" />
    <PackageVersion Include="Vortice.Vulkan" Version="3.2.1" />
    <!-- Vortice.ShaderCompiler (runtime GLSL->SPIR-V) is no longer referenced: shaders are pre-baked
         to SPIR-V by tools/BakeShaders (which pins its own version inline, outside this CPM scope). -->

  </ItemGroup>

</Project>
